# averosi

Personal link-hub homepage for `averosi.com`. Static, single-page, no
build step — deployed as-is via Cloudflare Pages.

## Deploy

Cloudflare Pages → Framework preset: `None` → Build command: *(empty)* →
Build output directory: `/`.
